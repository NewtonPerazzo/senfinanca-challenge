import { Container, TextHeader, TitleHeader } from "./style";

export default function Header(){
    return(
        <Container>
            <TitleHeader>SenFinança</TitleHeader>
            <TextHeader>Controle sua finança de forma fácil</TextHeader>
        </Container>
    );
}